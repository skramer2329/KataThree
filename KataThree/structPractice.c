#include <stdio.h>
#include <stdlib.h>
#include "structPractice.h"

void createList(node** head) {
	*head = NULL;
}

void addToEnd(node** head, node* endNode) {
	if (head == NULL)
		printf("Head is NULL");
	else {

	}
}